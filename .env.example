# Copy to .env.local (never commit .env.local)
# Vercel: paste the same keys into Project → Settings → Environment Variables

# Supabase public client keys
VITE_SUPABASE_URL=https://unnheqshkxpbflozechm.supabase.co
VITE_SUPABASE_ANON_KEY=

# Sentry (client)
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=production
